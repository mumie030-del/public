# 如何上传代码到 GitHub - 操作指南

## 📋 前提条件

✅ SSH 密钥已配置（已完成）
✅ Git 仓库已初始化（已完成）
✅ 远程仓库已连接：`git@github.com:mumie030-del/public.git`

---

## 🚀 日常上传代码的步骤

### 方法一：上传所有修改的文件

```bash
# 1. 进入项目目录
cd /root

# 2. 查看当前修改了哪些文件
git status

# 3. 添加所有修改的文件到暂存区
git add .

# 4. 提交修改（填写描述信息）
git commit -m "描述你的修改内容"

# 5. 推送到 GitHub
git push origin main
```

### 方法二：只上传特定文件

```bash
# 1. 进入项目目录
cd /root

# 2. 添加特定文件
git add CBAM/train.py
git add LTAE/module.py
# 或者添加整个文件夹
git add CBAM/

# 3. 提交修改
git commit -m "更新 CBAM 和 LTAE 模块"

# 4. 推送到 GitHub
git push origin main
```

---

## 📝 常用命令说明

| 命令 | 说明 |
|------|------|
| `git status` | 查看当前文件状态（哪些文件被修改了） |
| `git add .` | 添加所有修改的文件 |
| `git add 文件名` | 添加指定文件 |
| `git commit -m "说明"` | 提交修改并添加说明 |
| `git push origin main` | 推送到 GitHub |
| `git log --oneline` | 查看提交历史 |
| `git diff` | 查看文件具体修改了什么 |

---

## 💡 实际操作示例

### 示例 1：修改了训练脚本后上传

```bash
cd /root
git add CBAM/train.py
git commit -m "优化 CBAM 训练参数"
git push origin main
```

### 示例 2：添加了新模型后上传

```bash
cd /root
git add new_model/
git commit -m "添加新的分割模型"
git push origin main
```

### 示例 3：修改了多个文件后一次性上传

```bash
cd /root
git add .
git commit -m "更新所有模型的数据集处理代码"
git push origin main
```

---

## ⚠️ 注意事项

1. **每次修改后都要先 `git add`，再 `git commit`，最后 `git push`**
2. **commit 信息要写清楚**，方便以后查看修改历史
3. **推送前可以用 `git status` 检查**，确保添加了正确的文件
4. **不要上传敏感信息**（密码、密钥等）

---

## 🔧 如果遇到问题

### 问题 1：推送被拒绝（rejected）

```bash
# 先拉取远程更新
git pull origin main

# 再推送
git push origin main
```

### 问题 2：忘记添加文件就提交了

```bash
# 添加遗漏的文件
git add 遗漏的文件

# 修改上一次提交（追加文件）
git commit --amend --no-edit

# 推送
git push origin main
```

### 问题 3：想撤销某个文件的修改

```bash
# 撤销对某个文件的修改（恢复到上次提交的状态）
git checkout -- 文件名
```

---

## 📦 快捷脚本（可选）

创建一个快速上传脚本：

```bash
# 创建脚本
cat > /root/quick_push.sh << 'EOF'
#!/bin/bash
cd /root
git add .
echo "请输入提交说明："
read message
git commit -m "$message"
git push origin main
echo "✅ 上传完成！"
EOF

# 添加执行权限
chmod +x /root/quick_push.sh
```

以后只需运行：
```bash
/root/quick_push.sh
```

---

## 🌐 查看你的 GitHub 仓库

访问：https://github.com/mumie030-del/public

---

**记住这三步：add → commit → push** 🎯

